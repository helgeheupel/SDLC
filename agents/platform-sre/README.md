# Platform/SRE Agent

**Agent file moved to:** `.claude/agents/hh-platform-sre.md`

Load this agent with: `claude --agent hh-platform-sre`

The `reference/` directory remains here for version-controlled reference materials that the agent reads on demand.
