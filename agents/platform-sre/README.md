# Platform/SRE Agent

**Agent file moved to:** `.claude/agents/platform-sre.md`

Load this agent with: `claude --agent platform-sre`

The `reference/` directory remains here for version-controlled reference materials that the agent reads on demand.
