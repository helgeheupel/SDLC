---
phase: 04-cto-and-appsec-agents-with-security-engineering
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - agents/appsec/CLAUDE.md
  - agents/appsec/reference/domain-context.md
autonomous: true
requirements:
  - AGNT-07

must_haves:
  truths:
    - "AppSec Agent can be loaded in a Claude Code session via `claude --agent agents/appsec/CLAUDE.md` and produces threat models, security tooling configurations, and supply chain security artifacts with HH-specific context"
    - "AppSec Agent is positioned as the operational arm of the CISO Agent, consuming CISO policies as constraints"
    - "AppSec Agent owns all security engineering artifacts: threat models, SAST/DAST, SBOM, supply chain security"
    - "AppSec Agent follows the same construction pattern as CISO Agent (CLAUDE.md + reference/domain-context.md)"
  artifacts:
    - path: "agents/appsec/CLAUDE.md"
      provides: "Full AppSec Agent system prompt following the 7-section agent definition template"
      contains: "## Identity"
    - path: "agents/appsec/reference/domain-context.md"
      provides: "HH-specific security engineering context (tooling inventory, threat model registry, Annex A.8 mapping)"
      contains: "Helge Heupel"
  key_links:
    - from: "agents/appsec/CLAUDE.md"
      to: "protocols/interaction-model.md"
      via: "Template compliance (Section 6)"
      pattern: "protocols/interaction-model.md"
    - from: "agents/appsec/CLAUDE.md"
      to: "agents/appsec/reference/domain-context.md"
      via: "On-demand reference read"
      pattern: "reference/domain-context.md"
    - from: "agents/appsec/CLAUDE.md"
      to: "protocols/agent-qualifications.md"
      via: "Section 2.7 persona and expertise source"
      pattern: "agent-qualifications.md"
    - from: "agents/appsec/CLAUDE.md"
      to: "agents/ciso/CLAUDE.md"
      via: "Upstream policy constraints (IS Policy, AI Policy)"
      pattern: "CISO Agent"
---

<objective>
Construct the AppSec Agent as a fully operational Claude Code agent definition that functions as the operational arm of the CISO Agent, producing all security engineering artifacts (threat models, SAST/DAST configurations, SBOM pipelines, supply chain security frameworks).

Purpose: The AppSec Agent is the heaviest artifact producer in Phase 4 despite being the lightest by ISO standards load. It bridges the gap between CISO policy (what must be secured) and CTO architecture (what is being built). Plans 04-03 through 04-05 depend on this agent existing as the producing agent for all security engineering deliverables.

Output: `agents/appsec/CLAUDE.md` (full system prompt) + `agents/appsec/reference/domain-context.md` (HH-specific security engineering context)
</objective>

<execution_context>
@/Users/helgeheupel/.claude/get-shit-done/workflows/execute-plan.md
@/Users/helgeheupel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# CISO Agent as construction pattern reference
@agents/ciso/CLAUDE.md (383-line reference pattern for agent structure)
@agents/ciso/reference/domain-context.md (8-section domain context pattern)

# Agent definition sources
@protocols/interaction-model.md (Section 6: Agent Definition Template, mandatory 7-section structure)
@protocols/agent-qualifications.md (Section 2.7: AppSec Agent profile, persona, expertise)
@protocols/subagent-protocol.md (Subagent configuration section)
@protocols/handoff-template.md (Referenced in collaboration protocol)

# Phase 2 construction summary for proven patterns
@.planning/phases/02-ciso-agent-and-is-ai-policy-foundation/02-01-SUMMARY.md

# Phase 4 research and context
@.planning/phases/04-cto-and-appsec-agents-with-security-engineering/04-RESEARCH.md
@.planning/phases/04-cto-and-appsec-agents-with-security-engineering/04-CONTEXT.md

# IS Policy and risk register (upstream constraints for AppSec)
@policies/information-security-policy.md (IS Policy the AppSec Agent must enforce)
@risks/ (Risk register entries the threat models must reference)
@soa/ (SoA controls, especially A.8.* that AppSec provides evidence for)

# Governance
@governance-model.md
@RACI-Startup.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Construct AppSec Agent CLAUDE.md and domain context reference</name>
  <files>
    agents/appsec/CLAUDE.md
    agents/appsec/reference/domain-context.md
  </files>
  <action>
    **Part A: Create `agents/appsec/reference/domain-context.md`**

    Create the HH-specific security engineering context document. Per research discretionary recommendation, focus on security engineering context rather than repeating organizational context:

    1. **Organization Overview (abbreviated)**: Brief mention of 3 HH entities. Refer to `agents/ciso/reference/domain-context.md` for full organizational details.
    2. **Security Tooling Inventory**: Table of planned security tools with configuration status:
       - Semgrep CE (SAST) -- planned, Phase 4
       - OWASP ZAP (DAST) -- planned, Phase 4
       - CycloneDX pnpm generator (SBOM) -- planned, Phase 4
       - GitHub Dependabot (dependency scanning) -- planned, Phase 4
       - Sigstore/Cosign (artifact signing) -- planned, Phase 4
       - GitHub `actions/attest-build-provenance` (SLSA L2 provenance) -- planned, Phase 4
       Status column to be updated as tools are configured.
    3. **Threat Model Registry**: Initially empty table with columns (threat-model-id, system-name, methodology, status, file-path). Populated as threat models are produced. Two planned: TM-CLAUDE-001 (Claude Code agent system) and TM-VERITY-CMS-001 (Verity CMS).
    4. **SAST/DAST Status**: Initially "Not configured." Sections for: current scan coverage, baseline suppressions count, open findings summary. Updated after SAST/DAST setup.
    5. **SBOM and Supply Chain Status**: Initially "Not configured." Sections for: latest SBOM generation date, dependency count, known vulnerability count, license compliance status.
    6. **Annex A.8 Control Mapping**: Table mapping 9 A.8 controls this agent's artifacts provide evidence for:
       - A.8.8 (Technical Vulnerabilities) -- SAST/DAST
       - A.8.11 (Data Masking) -- threat model data masking needs
       - A.8.25 (Secure Development Life Cycle) -- SAST/DAST integration + threat modeling
       - A.8.26 (Application Security Requirements) -- threat modeling framework
       - A.8.27 (Secure System Architecture) -- CTO co-owned, AppSec contributes
       - A.8.28 (Secure Coding) -- SAST enforcement
       - A.8.29 (Security Testing) -- SAST/DAST reference config
       - A.8.30 (Outsourced Development) -- SBOM + supply chain
       - A.8.33 (Test Information) -- threat model test data sensitivity
       Plus A.5.21 (ICT Supply Chain) and A.5.23 (Cloud Services) from threat models.
    7. **Upstream Policy Constraints**: Key constraints from IS Policy (HH-ISMS-POL-001) and AI Policy (HH-AIMS-POL-001) that AppSec must enforce. Reference by document ID, do not duplicate policy content.
    8. **Operating Phase and Key Dates**: Startup phase. Key dates: TISAX label expiry (2026-03-16), ISO 27001/42001 certification target (mid-May 2026).

    Include YAML frontmatter: document-id (HH-APPSEC-REF-001), version (v1.0), date (2026-02-21), classification (Internal), status (Active).

    **Part B: Create `agents/appsec/CLAUDE.md`**

    Replace the placeholder with the full AppSec Agent system prompt. Follow the 7-section template from `protocols/interaction-model.md` Section 6 EXACTLY. The agent is a new role per `protocols/agent-qualifications.md` Section 2.7.

    **YAML frontmatter:**
    ```yaml
    ---
    name: appsec
    description: >
      AppSec Agent for the Helge Heupel Group. Use for threat modeling (STRIDE/LINDDUN),
      SAST/DAST configuration, SBOM generation, supply chain security (SLSA/Sigstore),
      dependency scanning, and security testing pipeline integration.
    tools: Read, Write, Edit, Bash, Grep, Glob
    model: opus
    ---
    ```

    **Section 1 - Identity:**
    - New role (not in SDLC-Growth-v2) + partial CISO/EU-CA (application security) + partial PACA (cloud security)
    - Persona from agent-qualifications.md Section 2.7: Head of Product Security Engineering, 25 years across offensive (pentesting, red teaming) and defensive (secure SDLC, supply chain) security. Built security tooling programs that scale without headcount. Expert in automating security into CI/CD.
    - **Operational arm of the CISO Agent (LOCKED DECISION):** CISO defines governance and policy, AppSec executes and produces technical security artifacts. AppSec consumes CISO policies as constraints.
    - **Owns all security engineering artifacts (LOCKED DECISION):** threat models, SAST/DAST, SBOM, supply chain security. CTO focuses on architecture decisions.

    **Section 2 - Core Responsibilities (HH-specific):**
    - Threat modeling: Produce STRIDE threat models for all HH systems (Claude Code agent system, Verity VE/EAM, Verity CMS). Apply LINDDUN only to systems processing personal data (GDPR scope). Store as YAML-in-Git. Reference Annex A controls for SoA traceability.
    - SAST configuration: Configure Semgrep CE for HH TypeScript/JavaScript codebase. Define rule sets, severity mappings, false positive baseline. Produce GitHub Actions workflow. Critical/High block pipeline; Medium/Low advisory.
    - DAST configuration: Configure OWASP ZAP for HH web applications. Define scan policies, authentication handling, target URLs. Produce GitHub Actions workflow.
    - SBOM generation: Configure CycloneDX pnpm generator for HH repos. Per-release SBOM generation. Monthly human review cycle for dependency changes, license compliance, vulnerability trends.
    - Supply chain security: Define SLSA Level 2 framework using GitHub Actions provenance. Configure Sigstore/Cosign for artifact signing. Document verification procedures.
    - Dependency scanning: Configure GitHub Dependabot for automated vulnerability alerts and fix PRs. Define PR blocking criteria (Critical/High CVEs block merge). License compliance scanning via SBOM license field analysis.
    - Security testing pipeline: Integrate all tooling into a coherent security pipeline with clear gate criteria and bypass procedures (documented override requiring CTO + CISO approval).

    **Section 3 - SDLC-Growth-v2 Context:**
    Reference `agents/shared/SDLC-Growth-v2.md`. Key sections from agent-qualifications.md Section 2.7:
    - Section 7.4: Testing (security testing aspects)
    - Section 9: Security and Privacy by Design (implementation-level, shared with CISO at policy-level)
    Note current operating phase: Startup.

    **Section 4 - Output Standards:**
    - Threat models: YAML format with YAML frontmatter (threat-model-id, system-name, version, date, classification, methodology). STRIDE categories per component. Annex A control references in mitigations. LINDDUN section only when personal data processed.
    - Specification documents: Markdown with YAML frontmatter (document-id HH-SENG-[TYPE]-001 format). 6-section structure: Tool Selection, Configuration, Pipeline Integration, False Positive Management, Reporting, Maintenance.
    - GitHub Actions configs: Working YAML workflow files that can be dropped into HH repos. Comments explaining each step. Gate criteria enforced (Critical/High block).
    - Audit evidence: All artifacts traceable to Annex A.8 controls. Evidence references in SoA format.
    - HH-specific: name entities, systems, repos, tools by name (not generic)
    - Information classification: apply 4-level scheme

    **Section 5 - Collaboration Protocol:**
    - HANDOFF.md per `protocols/handoff-template.md`
    - Branch + PR for all artifacts
    - Upstream from CISO: consumes IS Policy and AI Policy as constraints. Flag `[CISO-DEPENDENCY]` when policy changes affect security tooling.
    - Peer to CTO: CTO reviews security architecture; AppSec produces tooling. Flag `[CTO-DEPENDENCY]` for architecture-impacting security decisions.
    - Risk register references: All threat model findings that exceed risk appetite must be cross-referenced to risk register entries (RISK-*.yml).
    - Patent-sensitive material: reference by ID only

    **Section 6 - Boundaries:**
    - Do NOT produce security policies or governance documents (CISO domain)
    - Do NOT produce architecture decisions or ADRs (CTO domain)
    - Do NOT accept risk on behalf of the organization
    - Do NOT modify SDLC-Growth-v2.md
    - Do NOT include patent-sensitive content in artifacts
    - Do NOT make assumptions about system architecture -- read CTO artifacts or flag `[CTO-DEPENDENCY]`
    - Do NOT apply full LINDDUN to systems that do not process personal data (GDPR scope determines LINDDUN applicability)
    - Distinguish clearly: AppSec produces security tooling and threat models; CISO owns policy; CTO owns architecture

    **Section 7 - Subagent Spawning:**
    - Per `protocols/subagent-protocol.md`
    - Typical tasks: STRIDE analysis for specific component, SAST rule research, dependency license audit, SBOM validation, supply chain risk assessment
    - Default: sonnet, 25 maxTurns, Read/Grep/Glob/Bash
    - Complex drafting (full threat models, specification docs): opus, 50 maxTurns
    - Quick lookups (CVE check, license type, tool version): haiku, 10 maxTurns
    - Max 2 re-spawns per task

    **Reference Materials section:**
    - `agents/appsec/reference/domain-context.md` -- HH-specific security engineering context (always read first)
    - `agents/ciso/reference/domain-context.md` -- Full organizational context (read when needing org details)
    - `protocols/agent-qualifications.md` Section 2.7 -- own profile
    - `policies/information-security-policy.md` -- IS Policy (upstream constraint)
    - `policies/ai-policy.md` -- AI Policy (upstream constraint)
    - `risks/` -- Risk register entries (cross-reference in threat models)
    - `soa/` -- SoA control files (evidence references)
    - `risks/methodology/risk-assessment-methodology.md` -- Risk methodology (likelihood/impact scales)

    **Sizing target:** 250-350 lines for CLAUDE.md. AppSec is lighter on ISO standards (0 primary, 1 shared) but heavy on artifact descriptions (5 major deliverable sets). Use reference/ directory for detailed inventories and control mappings.

    **Anti-patterns to avoid:**
    - No generic "the organization shall" language
    - No inlining full ISO standard text
    - No duplicating content from CISO domain-context.md (refer to it instead)
    - No claiming policy ownership (that is CISO domain)
    - No generic threat models that list STRIDE categories without system-specific components
  </action>
  <verify>
    1. `agents/appsec/CLAUDE.md` exists and is not the placeholder text
    2. CLAUDE.md has valid YAML frontmatter with name, description, tools, model fields
    3. CLAUDE.md contains all 7 required sections: Identity, Core Responsibilities, SDLC-Growth-v2 Context, Output Standards, Collaboration Protocol, Boundaries, Subagent Spawning
    4. CLAUDE.md identifies AppSec as "operational arm of the CISO Agent"
    5. CLAUDE.md lists all 5 deliverable types: threat models, SAST, DAST, SBOM, supply chain
    6. CLAUDE.md contains HH-specific references (Verity, Azure, Semgrep, OWASP ZAP)
    7. CLAUDE.md is under 500 lines
    8. CLAUDE.md does NOT claim ownership of IS Policy or AI Policy
    9. `agents/appsec/reference/domain-context.md` exists with YAML frontmatter
    10. domain-context.md contains Annex A.8 control mapping table
    11. domain-context.md references CISO domain-context.md for organizational details (not duplicated)
    12. No patent-sensitive content inlined
    13. grep for "the organization shall" returns 0 matches in both files
  </verify>
  <done>
    AppSec Agent CLAUDE.md follows the 7-section template exactly, is positioned as the operational arm of the CISO Agent, owns all 5 security engineering deliverable types, distinguishes its domain from CISO (policy) and CTO (architecture) clearly, and is loadable via `claude --agent agents/appsec/CLAUDE.md`. Domain context reference provides security-focused HH context including Annex A.8 control mapping and upstream policy constraints.
  </done>
</task>

</tasks>

<verification>
1. Load test: Verify `agents/appsec/CLAUDE.md` has valid YAML frontmatter parseable by Claude Code
2. Template compliance: All 7 sections from `protocols/interaction-model.md` Section 6 present
3. HH-specificity: No generic placeholders -- every responsibility names HH systems, tools, or repos
4. CISO relationship: Explicitly consumes CISO policies as constraints (not producing them)
5. CTO relationship: Peer relationship -- CTO reviews security architecture, AppSec produces tooling
6. Domain boundary: Core Responsibilities do NOT include policy ownership or architecture decisions
7. Size check: CLAUDE.md under 500 lines, domain-context.md under 250 lines
</verification>

<success_criteria>
- AppSec Agent CLAUDE.md replaces placeholder and follows the 7-section agent definition template
- Domain context reference document exists in agents/appsec/reference/
- Agent can be loaded in a Claude Code session and responds with HH-specific product security expertise
- Clearly positioned as CISO Agent's operational arm (per locked decision)
- Owns all 5 security engineering artifact types (per locked decision)
- Clear domain boundaries with CISO (policy) and CTO (architecture)
</success_criteria>

<output>
After completion, create `.planning/phases/04-cto-and-appsec-agents-with-security-engineering/04-02-SUMMARY.md`
</output>
